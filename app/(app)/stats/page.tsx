import { redirect } from "next/navigation";

/**
 * «Tag statistics» переехала на экран тегов.
 *
 * Редирект, а не копия: два пункта меню про одни и те же теги заставляли ходить
 * туда-обратно — посмотреть долю в одном месте, переименовать в другом.
 * Старые ссылки продолжают работать.
 */
export default function StatsPage() {
  redirect("/tags");
}
