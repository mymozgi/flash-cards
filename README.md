# Memorizer

*by Oleg Tsykhonia*

Личный тренажёр интервального повторения: карточки с текстом и изображениями,
батч-импорт из CSV, темы и теги, планирование по FSRS.

Спецификация: https://claude.ai/code/artifact/84853df6-1a8b-4f60-9652-0fe0eb510c6c
Проектные договорённости и скилы: [CLAUDE.md](CLAUDE.md)

## Что уже работает (этап 1 — ядро)

- Вход по email и паролю, единственный аккаунт, гость отправляется на `/login`
- Карточки: создание, правка, приостановка, мягкое удаление, обратная карточка
- Темы — дерево до трёх уровней, теги — плоские и независимые
- Повторение по FSRS: четыре оценки с подписанными интервалами, отмена, свайпы, клавиши
- Библиотека: фильтры по теме и тегу, полнотекстовый поиск, массовые операции

**Этап 2 — изображения:** загрузка файлом, перетаскиванием, вставкой из буфера
и камерой; сжатие в WebP на клиенте; до четырёх на сторону; просмотр во весь
экран; ежедневная уборка брошенных файлов.

**PWA:** приложение ставится на домашний экран телефона. Офлайн работает
оболочка и офлайн-заглушка; повторение без сети — этап 4.

**Этап 3 — батч-импорт:** мастер из четырёх шагов (файл → колонки → предпросмотр →
результат), автоопределение разделителя, маппинг колонок, поиск дублей, отчёт об
ошибках в CSV, откат в течение суток. Экспорт в CSV и JSON — там же, на `/import`.

**Варианты ответа:** колонки `choice1..choice3` в CSV и поля в редакторе. Режим
выбора ответа в сессии повторения — следующим шагом.

Интерфейс приложения английский; документация и комментарии в коде — русские.

## Запуск

1. Создайте проект на [supabase.com](https://supabase.com) (регион поближе, тариф Free).
2. SQL Editor → выполните `supabase/migrations/0001_init.sql`.
3. Authentication → Sign In / Providers → **выключите** «Allow new users to sign up».
4. Authentication → Users → Add user: свою почту и пароль. Это единственный аккаунт.
5. Project Settings → API: скопируйте URL и публичный ключ в `.env.local`
   (шаблон — `.env.example`).
6. `npm run check:db` — проверит, что схема на месте и закрыта для гостя.
7. `npm run dev` → http://localhost:3000

### Проверка базы

`npm run check:db` запрашивает таблицы публичным ключом, то есть от лица гостя.
Ожидаемый результат — «доступ закрыт» по каждой таблице: значит, она существует,
а права выданы только вошедшему пользователю. Успешное чтение без входа — повод
перепроверить RLS.

Чтобы дополнительно прогнать сквозной сценарий (вход, создание карточки, проверка
триггера расписания, уборка за собой), задайте переменные окружения перед запуском:

```powershell
$env:KARTOTEKA_EMAIL='you@example.com'; $env:KARTOTEKA_PASSWORD='...'; npm run check:db
```

## Ветки: локально сначала, на прод осознанно

Push в ветку прода = боевой деплой. Чтобы это были разные действия:

Vercel → Settings → Git → **Production Branch** поменять на `production`.
После этого:

    git push origin master          # Preview: свой адрес, прод не трогает
    git push origin master:production   # прод, когда проверено

Preview-адрес открывается с телефона и работает как настоящий — на нём и
стоит проверять перед прод-выкатом.

**База при этом одна.** Preview, прод и локальная разработка ходят в один
проект Supabase, поэтому «протестировать» на превью — это всё ещё работа с
боевыми данными. Отдельная база под разработку стоит $0 (на free-тарифе
Supabase даётся два проекта), но требует второго набора ключей в Vercel.

## Деплой на Vercel

1. New Project → Import Git Repository → этот репозиторий. Framework Next.js
   определяется сам, команды сборки менять не нужно.
2. Environment Variables — все четыре из `.env.example`, на окружения
   Production, Preview и Development сразу:

   | Переменная | Зачем |
   |---|---|
   | `NEXT_PUBLIC_SUPABASE_URL` | обязательна, без неё не соберётся |
   | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | обязательна |
   | `SUPABASE_SERVICE_ROLE_KEY` | только для ночной уборки файлов; без неё приложение работает, а `/api/cron/sweep` отвечает отказом и объясняет причину |
   | `CRON_SECRET` | любая случайная строка; Vercel шлёт её в заголовке `Authorization`, иначе маршрут вернёт 401 кому угодно |

3. Supabase → Authentication → URL Configuration: вписать боевой адрес в
   **Site URL** и в **Redirect URLs**. Без этого письмо подтверждения и
   восстановление пароля уводят на `localhost`.
4. Deploy. Крон из `vercel.json` запускается в 03:00 UTC — на Hobby это
   ровно один запуск в сутки, больше тариф не даёт.

Тариф Hobby — только некоммерческое использование.

## Установка на телефон

Приложение — PWA: манифест и service worker уже на месте, ставить ничего не
нужно. После деплоя открыть боевой адрес в браузере телефона:

- **iPhone, Safari** — Поделиться → «На экран «Домой»». Только Safari: из
  Chrome на iOS установка не предлагается.
- **Android, Chrome** — меню → «Установить приложение».

Иконка запускает приложение без адресной строки, долгое нажатие даёт ярлыки
Review и My flashcards. Работает только по HTTPS — на `localhost` с телефона
установка не предложится.

## Клавиатура

`Пробел` — показать ответ · `1`–`4` — оценка · `Z` — отменить последнюю оценку

## Стек

Next.js 16 (App Router, server actions) · Supabase (Postgres + Auth, RLS) ·
Tailwind CSS 4 · ts-fsrs · TypeScript
