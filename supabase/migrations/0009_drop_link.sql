-- Flashcards — свойство Link убрано из карточки.
--
-- ВНИМАНИЕ: миграция уничтожает данные. Если в каких-то карточках ссылки
-- сохранены, они пропадут. Приложение колонку больше не читает и не пишет,
-- поэтому запускать эту миграцию не обязательно — можно оставить колонку
-- лежать. Выполняйте, только если уверены, что ссылки не нужны.

alter table cards drop constraint if exists cards_link_url_check;
alter table cards drop column if exists link_url;
