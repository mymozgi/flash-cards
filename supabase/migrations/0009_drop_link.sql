-- ОТМЕНЕНО. Ничего не делает.
--
-- Миграция удаляла колонку cards.link_url вместе со всеми сохранёнными
-- ссылками. Её просили не запускать, и это оказалось верным: поле вернулось
-- как Source — «откуда это знание» — вместе с разделом Knowledge. См. 0018.
--
-- Файл оставлен пустым, а не удалён: номера миграций — это история, и дырка
-- в ней заставила бы гадать, что здесь было.

select 1;
